const express = require("express");
const route = express.Router();
const Campaign = require("./controller/CampaignController");


// route.post("/v1/linkedin/campaign/run",Campaign.run);

// route.post("/v1/linkedin/getlist",Campaign.Getlist);

module.exports = route;
